<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="viewModel"
            type="com.vside.app.feature.mypage.BookShelfViewModel" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white"
        tools:context=".feature.mypage.BookShelfActivity">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/cl_action_bar"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            app:layout_constraintTop_toTopOf="parent">
            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/ic_back"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                android:onClick="@{() -> viewModel.onBackClick()}"
                android:padding="18dp"
                android:contentDescription="@string/back" />
            <TextView
                android:id="@+id/tv_book_shelf_cnt"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toStartOf="@id/tv_book_shelf_title"
                app:layout_constraintHorizontal_chainStyle="packed"
                android:textColor="@color/main500"
                android:text="@{viewModel.scrapList.size() + ` `}"
                tools:text="25 "
                style="@style/TextAppearance.AppCompat.XL_EB"/>
            <TextView
                android:id="@+id/tv_book_shelf_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:layout_constraintStart_toEndOf="@id/tv_book_shelf_cnt"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                android:text="@{viewModel.scrapList.size() > 1 ? `books` : `book`}"
                tools:text="books"
                android:textColor="@color/g950"
                style="@style/TextAppearance.AppCompat.XL_EB"/>
        </androidx.constraintlayout.widget.ConstraintLayout>

        <include
            android:id="@+id/layout_loading"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintTop_toBottomOf="@id/cl_action_bar"
            app:layout_constraintBottom_toBottomOf="parent"
            layout="@layout/layout_loading"/>

        <androidx.recyclerview.widget.RecyclerView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintTop_toBottomOf="@id/cl_action_bar"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            android:orientation="vertical"
            android:paddingTop="12dp"
            android:paddingBottom="24dp"
            android:clipToPadding="false"
            bind:dino_items="@{viewModel.scrapList}"
            bind:dino_itemLayout="@{@layout/item_filter_tab_main}"
            bind:dino_eventHolder="@{viewModel}"
            tools:listitem="@layout/item_filter_tab_main"
            android:paddingHorizontal="@dimen/default_horizontal_margin"
            bind:dino_itemSpace="@{`14dp`}"
            app:layout_constraintBottom_toBottomOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>